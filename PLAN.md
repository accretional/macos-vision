1. create a ocr/ directory, move the ocr functionality into a subcommand with the ocr logic implemented in its own file (ocr.swift), rename the original ocr.swift to main.swift, refactor the original main.swift to perform common I/O and delegate to a subcommand fed into a switch statement (currently just ocr) 1.5 write a test/validation on the original sources and output, make sure they still work. 2. reimplement Sources/ocr/ocr.swift and Sources/main.swift in objective c and make sure the sources output match the current sources + output (add tests) 3. next we'll add many more subcommands, try adding a noop or debug subcommand with the same structure (its own Sources/Debug/ directory containing the logic, its own tests, its own case in the switch statement) to make sure this will scale to more files